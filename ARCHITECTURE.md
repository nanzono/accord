# accord の構造

## 俯瞰

accord は、職務経歴書やプロフィールの元になる経歴の記録（正本）と、そこから作る文書とを食い違わせないための、手元で動くサーバーです。正本は人が読み書きする Markdown のまま置き、accord がするのは、文章を書くための材料を渡すことと、決まりに反する書き込みを断ることと、全体を検査して食い違いを一覧にすることです。文面そのものは作らず、引き受けるのは機械で白黒の付く一致だけで、ネットワークには出ずに手元のファイルだけを見ます。

## 入口

入口は 2 つあり、どちらも下の「地図」に書く同じ層を通ります。

**MCP のサーバーとして起動する。** `accord` を引数なしで実行すると、標準入出力で応答を待ちます。公開するツールと資源の定義は、`server/` の `create_server` が設定から組み立てます。AI から使うときはこちらです。

**Python から直接呼ぶ。** 設定を読み（`vocabulary/` の `Settings`）、決まりを適用する層のクラス（たとえば `ConsistencyService`）をそのまま呼びます。手元のファイルを検査するときや、動きを確かめるときはこちらです。

どちらの入口でも、起動時に読むのは設定ファイル 1 つです。そこに、正本の置き場と、利用者自身の言葉（媒体の名前、分類の語）が書いてあります。

## 地図

層は 4 つで、依存は上から下への一方向だけです。

| 層 | 置き場 | 役目 |
|---|---|---|
| 公開の口 | `server/` | MCP のツールと資源を公開する。引数の型を確かめ、戻り値を整えるだけで、判断はしない |
| 決まりの適用 | `services/` | 材料をまとめ、書き込みの可否を決め、全体を検査する。`MaterialService`・`PositioningService`・`OfferingService`・`PublicRecordService`・`ConsistencyService` |
| 読み書き | `repository/` | Markdown を節に切り、欄を読み、行を足す。`MarkdownRepository` と `Section` |
| 正本 | Markdown ファイル | 人が直接読み書きする本体 |

層をまたいで使うものが 3 つあります。`ontology.yaml` が型・関係・ルールの定義で、型とルールについてはこの 1 ファイルだけが定義の置き場です（上の表の「正本」は経歴の記録のことで、別物です）。`models/` は、そこから生成した型のコードと、戻り値の型（`Rejection`・`NextAction`・`WriteResult`・`Violation`・`Material`・`ConsistencyReport` など）を持ちます。`vocabulary/` は設定ファイルを読みます。

このほか、定義から型のコードを書き出す生成器が `scripts/`、架空の設定とサンプルデータが `samples/`、受け入れ条件のテストが `tests/` にあります。

Markdown の書式を知っているのは、読み書きの層だけです。見出しの深さが変わっても、箇条書きが表になっても、直すのはこの層に限られます。決まりを適用する層は、節と欄しか見ません。

## 意図的に無いもの

- **逆参照。** 関係の矢印は一方向で、職歴や案件の側に「これはあのパッケージで使っている」と書き足すことはしません。実行時の検査ではなく、層の分け方と正本の書き方で守ります。
- **文面の生成。** プロフィールや応募文の文章を書く機能はありません。良し悪しを機械で判定できないものは引き受けません。
- **実データ。** このリポジトリに出てくる人物・会社・媒体の名前はすべて架空です。架空であることを確かめた記録は `samples/README.md` にあります。人によって違う言葉（媒体の名前、分類の語）は型の定義に書き込まず、設定ファイルから読みます。
- **書式を知る層が 2 つ以上あること。** Markdown の書式を知る場所を増やすと、正本の書き方が変わったときに直す場所が散ります。

## 横断の事柄

**足跡（provenance）。** 検査の戻り値の先頭に、読んだ設定ファイルの場所、適用した読み方の指定、動いているソースの置き場と版が付きます。組み立てるのは決まりの適用の層です。診断の情報は、公開の口を通さない直接呼びでも同じように要るからです。

**設定で読み方を吸収する。** 見出しの深さ、箇条書きか表か、項目名の言い換えは、設定の読み方の節で指定します。正本のファイルを書き換えさせません。

**拒否は戻り値。** 決まりに反する入力は、例外ではなく、理由と次の一手を持つ値（`Rejection` と `NextAction`）で返します。読むだけの操作と検査は、ファイルを一切変えません。

**ID で結ぶ。** 項目どうしは、見出しの文字ではなく、人が振る短い ID で結びます。機械が見るのは、形と、重なりの無さと、指した先が実在することだけです。

それぞれをこう決めた理由は `docs/decisions/` にあります。
